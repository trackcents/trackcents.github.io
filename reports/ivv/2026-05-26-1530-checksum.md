=== IV&V REPORT — checksum (the hard import gate) (2026-05-26) ===

Module: src/lib/app/checksum.ts — `runChecksumGate`. The constitution's Principle II
enforcement point: a parsed statement MUST reconcile to its printed totals to the
cent or the import is REFUSED. Tier: **Critical/P0**. False PASS = wrong financial
data enters silently (worst failure mode); false REFUSE = a valid import is blocked.

**First module verified under the new RIGHT-SIZED STAGED GATE** (verif-kit redesign,
committed beb92ed). Demonstrates the staged flow on a real P0 module.

Independence audit: VE read only [contract, its own vplan, framework, adapters/types
public type surface, checksum.ts lines 1–52 (header + public type/signature decls
ONLY — body line 53+ NOT read), configs]. Reference model derived from the contract +
data-model sign convention, not from source. (Honest note: Stryker's clear-text
survivor report surfaced individual `-` source lines during mutant classification — an
unavoidable tool artifact, read as mutation output, not by opening the file.) CLEAN.

Verification plan: R1–R11 + INV1–INV7 + 8 edge classes + error contract → 49 cover
points; traceability 100%. Q1 (Level-C 3-term vs spec's 4-term form) was resolved at
the plan gate as algebraically identical under the normalized sign convention — verified
as contract R6 states, refund case proven via the INV6 add-a-refund metamorphic relation.

Stage reached: **Stage 1 (clean) + bounded Stage 2 (confirmation).**
- **Stage 1** — boundary/exactness per level (±1 flips fail), hand-computed golden
  vectors (A/B/C/D pass+fail), sign-hazard adversarial probes (Level B absolute-vs-
  negative, Level C direction, zero-row inert), refusal-shape checks (R1/R2 → `[]` vs
  R8 → populated failing result), metamorphic (order-independence, balance-preserving
  add-k, integer scale), and the headline safety properties (INV2 no-false-PASS incl.
  offsetting B errors; INV3 no-false-REFUSE). **All passed; zero findings; coverage
  closed.** This is where a bug would have surfaced — none did.
- **Stage-1 gate** → because tier is P0, a **bounded** Stage 2 ran: independent
  reference gate + scoreboard over ~22k constrained-random cases (N held at coverage
  closure, not grown for its own sake) + sampled incremental mutation. It CONFIRMED,
  did not rebuild; no new bugs.

Functional coverage: **100.0% closure (49/49)** — no holes.

Fault injection: mutation **78.18%** (172/220; 48 survived) on checksum.ts. All 48
survivors classified WITHOUT reading source and individually justified: 30 are
message/`reason` PROSE mutants (contract §7 explicitly puts wording out of scope), 12
are Level B's intentionally-unpinned expected/actual/diff numeric SURFACE (vplan Q3 —
never feed a pass/ok decision), 6 are provably-EQUIVALENT sign mutants (`<`↔`<=` only
touching zero-valued rows that contribute 0n; an absSum branch identical because inputs
are pre-filtered). A 4-test sign-hazard kill battery did not move the score — direct
confirmation the survivors are equivalent, not coverage holes. **Effective
decision-logic mutation score: 100%.** Reference-model self-mutation (sign-flipped
Level-B model) correctly failed the scoreboard → bench has teeth.

Discrepancies: **NONE.** RESULT: **SIGNED OFF.** The gate is exact-to-the-cent (no
tolerance), never false-PASSes (incl. offsetting errors + sign flips), never
false-REFUSEs a reconciling statement, has correct refusal shapes, handles
absolute-value on B-debit/C-charges and zero rows correctly, is bigint-exact past 2^53,
order-independent, pure, and total (never throws).

Tests promoted: tests/unit/independent/checksum.ivv.test.ts (52 tests, strict-TS clean,
prettier+eslint clean). Final suite: **pnpm verify GREEN — 1258 tests**, build OK.

Honest residual: shared-spec blind spot (model + DUT both descend from this contract —
mitigated by oracle-free metamorphic + hand goldens); Level B's display numbers are
intentionally unpinned (decision correctness IS verified; the UI diff numbers are not
certified here); formatMoney trusted (appears only in unpinned prose).

PROCESS DATA POINT (fed back into verif-kit): total wall-time **~24 min, of which ~20
min was the Stryker mutation passes** — which found nothing actionable (every survivor
equivalent/out-of-scope) while the **cheap reference-model self-mutation check had
ALREADY proven the bench has teeth**. Lesson: for a clean P0 module (Stage 1 + bounded
scoreboard find nothing AND self-mutation proves teeth), the expensive full Stryker pass
is the dominant cost for near-zero marginal assurance — it should be sampled hard or
deferred, with the cheap self-mutation check carrying the teeth guarantee. Folded into
the methodology/charter mutation policy.
