=== IV&V REPORT — money (parse/format utilities) (2026-05-26) ===

Module: src/lib/util/money.ts (`parseMoney`, `formatMoney`, `sumMoney`, `absMoney`).
Tier: Core, with one Critical surface — `parseMoney` is a parser of PDF-extracted
money strings on the money path; the rest are display/trivial helpers.

**Second run under the lean staged gate. Sized small ON PURPOSE — ~4 minutes total.**

RESULT: **SIGNED OFF at Stage 1, no bugs.** Independence clean (VE read the contract +
constitution Principle II + data-model + framework only; never money.ts or the Designer's
money tests). 39 cover points, 100% closure.

Stage 1 (the high-yield checks; no heavy reference model, no mutation marathon — per the
new policy for a 4-function module):
- **INV1 round-trip metamorphic** (the oracle-free headline): `parseMoney(formatMoney(c)) === c`
  over 500 random bigints incl. magnitudes > 2^53, + 10 directed boundaries. Clean.
- **parseMoney hazard goldens** (hand-derived from the contract): value forms, whole-dollars
  ×100, bare/padded fraction, sign (parens-neg, minus-neg, `(-5)`→`-500n` no double-negate),
  negative-zero→`0n`. Clean.
- **R7/INV2 reject set (no silent failure, Principle II):** all 13 cases throw —
  `""`, `" "`, `"$"`, `"-"`, `"."`, `"()"`, `"5."`, `"1.005"` (3-frac → throw, not truncate),
  `"1,23,456"` (bad grouping), `"(($100))"` (nested parens), junk. Clean.
- **formatMoney goldens** + currency rule (USD/omitted→`$`, other→no symbol). Clean.
- **Bounded fuzz** (1000 junk strings): parseMoney returns an exact round-tripping bigint
  OR throws — never a silent 0/NaN. 0 failing seeds.
- **Teeth check:** a throwaway corrupted golden + an off-by-one round-trip both correctly
  FAILED → the oracle is not vacuously passing. (Reference-model self-mutation substitute;
  full external Stryker deferred per the staged gate — proportionate for 4 small functions.)

Discrepancies: NONE. Tests promoted: tests/unit/independent/money.ivv.test.ts.
Final suite: **pnpm verify GREEN — 1278 tests**, build OK.

Honest residual: independence is strong here (oracles are round-trip + hand goldens, not a
mirror model, so Knight & Leveson correlated blind spots are minimal); a hard external
mutation score was not measured (the teeth-check is the proportionate substitute — re-spawn
with mutation enabled if a P0-floor number is ever required); fuzz is bounded, not exhaustive.

Process note: ~4 min wall-time for a clean Core module — vs the ~25 min the old
always-full-environment flow spent on comparable modules. The staged gate is delivering the
intended ~5–6× speedup with the same assurance on the high-yield checks.
