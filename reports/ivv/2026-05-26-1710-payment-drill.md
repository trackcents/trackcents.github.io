=== IV&V REPORT — payment-drill (US-P1-D drill-down orchestrator) (2026-05-26) ===

Module: src/lib/app/payment-drill.ts (`getPaymentDrill`). Tier: Core, user-facing
capstone of US-P1-D — click a "$X paid to credit card" bank row → see the specific
purchases that $X paid for, each with provenance back to its source statement row. Its
dependencies (reconciliation matcher + transaction-stream FIFO engine) are already
signed off; this module's OWN surface is the orchestration + provenance resolution +
null-guards.

**Lean staged run (thin orchestrator over verified parts): Stage 1 only, ~5 min.**

Independence: VE read only the contract + the sibling signed-off contracts + spec +
public type declarations; never the payment-drill/reconciliation/transaction-stream/
card-payment bodies. Collaborators driven transitively via real `ImportSuccess[]`
fixtures (NOT mocked) — the verified modules are trusted, not re-verified.

RESULT: **SIGNED OFF at Stage 1, no bugs.** 15 cover points, 100% closure.

Stage 1 (the high-yield checks):
- **Golden multi-statement drill** (the canonical case — a March payment paying off
  February's carried balance across TWO statements): refs correctly point into the
  EARLIER (Feb) import, txn 0 then txn 1; covered/uncovered/amount/card fields all
  correct. The index-confusion bug-class the contract flagged (import_index vs
  transaction_index, wrong-import resolution) — specifically clean.
- **R5/INV2 provenance round-trip (the oracle-free headline):** over 400 constrained-
  random multi-statement card histories, EVERY ref in `paid_for` resolves to its exact
  source purchase row, is a real debit (amount<0), on the SAME card — no ref out of
  range, duplicated, or cross-card/cross-import. Clean.
- **INV1 conservation pass-through:** `Σ covered + uncovered === payment_amount_minor`
  end-to-end (×402). Clean.
- **INV3 coverage bound; R1/R2/INV5 null-guards** (out-of-range / non-payment / CC-side
  row / null last-4 / empty imports / no CC import → null, never throws, 200 fuzzed
  index pairs); **INV4 determinism + purity** (args not mutated). All clean.
- **Teeth check:** a +1n off-by-one injected into the conservation oracle was caught
  immediately, then reverted. Full Stryker deferred per right-sizing (thin orchestrator,
  no bug found).

Discrepancies: NONE. Tests promoted: tests/unit/independent/payment-drill.ivv.test.ts
(22 tests). Final suite: **pnpm verify GREEN — 1349 tests**, build OK.

Honest residual: collaborators trusted (signed off separately) — a latent collaborator
bug would surface here only if it broke the round-trip/conservation identities (it did
not); the no-last-4 issuer drill path (confidence 0.85) is lightly covered (exercised in
guards, not a successful-drill property — it's the matcher's concern); full mutation
deferred; multi-currency/clock-skew not fuzzed (inherited stream assumptions).

=== US-P1-D PIPELINE: VERIFICATION COMPLETE ===
With this sign-off the entire signature-feature chain is independently verified
end-to-end: **card-payment** (detect issuer/last-4) → **reconciliation** (match payment
to card) → **transaction-stream** (FIFO: which purchases) → **payment-drill** (orchestrate
+ provenance) — plus the **checksum** import gate and the **money** parser. Across the
session, IV&V found and fixed **4 real P0-core bugs**: D19 (same-day attribution
determinism), D20 (card-key collision), D22 (matcher row-vs-statement ambiguity), D25
(issuer false-match). The only core module not independently verified is
`transaction-view.ts`, whose sole real hazard (the `::` key collision) is
production-unreachable and whose fix is user-gated (it changes the /transactions
filter-key contract).
