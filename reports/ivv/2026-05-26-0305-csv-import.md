=== IV&V REPORT — csv-import (+ export-csv round-trip) (2026-05-26) ===

Modules: src/lib/app/csv-import.ts, src/lib/app/export-csv.ts (US-IMP-CSV, US-P4-D)
Contract: specs/001-money-tracker-mvp/verification/csv-import.contract.md
VE: verification-engineer subagent (fresh context, agent a1fc9141), PLAN+EXECUTE, full run.

Independence audit: VE read only the contract, spec-tracker-research-features.md, DECISIONS D9/D10,
  the constitution, the PERSISTENCE contract (schema.ts/types.ts/store.ts — explicitly permitted, not
  the DUT), the framework, config, and an unrelated promoted file (conventions). DUT files: only the
  public interface declarations + `export` lines; NO function body opened (boundary behaviors learned
  black-box by calling the public functions). → CLEAN.

Verification plan: R1–R9 + INV1–INV5 + ~18 §6 edges + 10 §8 error cases → 93 cover points;
  traceability 100%.

Environment: fast-check generators (bigint cents ±1e14 + dense sub-dollar; RFC-4180 special/unicode
  cells; round-trip grids; money-string + raw-CSV fuzz) + independent from-spec reference model
  (refCentsToDecimal/refCsvEscape/refParseAmountToCents, integer-only rounding) + 4 scoreboards
  (~4800 comparisons, CLEAN) + INV1 cents round-trip identity (2000 runs) + INV2 RFC-4180 round-trip
  (1500) + INV3 throw-never-drop (3400) + INV5 sign/magnitude (600) + a PERSISTED corpus
  (corpus/csv-import/seed_known_corners.json: 19 amount + 10 CSV-structure golden seeds, re-run every
  suite).

Functional coverage: 100.0% (93/93). Holes: none.

Fault injection: mutation 83.57% total / 84.56% covered (export-csv 100%; csv-import 82.32%) — above
  the P1 floor (70) and the new-code target (80). Survivors justified (error-message string literals —
  §8 requires a message + row number, not exact prose; redundantly-guarded regex anchors with a second
  validation layer; behaviorally-equivalent boundary mutants; the optional `category` alias). 5
  unreachable defensive branches left unexercised by deliberate discipline. Reference-model
  self-mutation: scoreboard FAILED as expected on an off-by-one and a truncating (not half-up) model →
  bench has teeth.

Discrepancies: 0 confirmed bugs. 3 spec ambiguities logged for the architect (all defensible DUT
  behavior — contract-tightening, not fixes):
  - [D1] parseCsvRows of a spaces-only line returns [['   ']] not [] — R4's two clauses conflict for a
    non-empty-but-whitespace cell; DUT follows the RFC-faithful reading. ACCEPTED as-is.
  - [D2] importCsv trims leading/trailing whitespace from descriptions — R9 round-trip identity holds on
    its unambiguous domain (no edge whitespace); §6 says edge whitespace must be "handled"; trimming is
    defensible. ACCEPTED as-is; recommend R9 state "descriptions are trimmed".
  - [D3] negative half-cent rounds half-away-from-zero (sign-symmetric with the positive R5 example).
    ACCEPTED as-is; recommend R5 state the negative direction explicitly.
  Designer triage: all three are VE-MISREAD-adjacent / SPEC-AMBIGUOUS-but-defensible; the DUT behavior
  is the desired behavior, so no code change. Contract notes deferred to morning (BLOCKERS).

Tests promoted: tests/unit/independent/csv-import.ivv.test.ts (78 tests) + corpus moved to
  tests/unit/independent/corpus/csv-import/. Import depth fixed; prettier+eslint clean.

Final suite: pnpm verify GREEN (959 tests).

Honest residual (per VE): shared-spec blind spot (mitigated by INV1/INV2 identities, INV5 metamorphic,
  golden values, RFC-4180 first principles); no real-world bank-CSV corpus (synthetic+fuzz only — add a
  real CSV seed when available); 5 unreachable defensive branches; R8 checks the in-memory schema, not
  the OPFS serialize/deserialize round-trip (out of module scope).

Decision: ACCEPTED. csv-import + export-csv are the verification-of-record for US-IMP-CSV / US-P4-D.
